[project]
name = "tpcdsgen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datafusion>=53.0.0",
    "pyarrow>=24.0.0",
]
